version: 2

models:
  - name: dim_date
    description: Conformed Date Dimension. One row per day.
    columns:
      - name: datekey
        description: Surrogate key YYYYMMDD
        tests: [not_null, unique]
      - name: date
        description: The actual date value
        tests: [not_null, unique]
